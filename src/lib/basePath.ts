// The deploy base path (e.g. "/asistenteperso" for a GitHub Pages project site).
// Empty for local dev and root/custom-domain deploys. Used for the manifest,
// service worker, and any absolute asset URLs.
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH || "";
